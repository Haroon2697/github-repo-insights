# Local production-style run (backend only; MongoDB = Atlas URI in .env).
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    environment:
      NODE_ENV: production
    restart: unless-stopped
